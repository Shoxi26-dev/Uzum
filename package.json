{
  "name": "uzum",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 8080",
    "card-server": "json-server --watch products_uzum.json --port 3000",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "swiper": "^12.0.2",
    "toastify-js": "^1.12.0"
  }
}
